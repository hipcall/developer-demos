version: '3.8'

services:
  cdr-app:
    build: .
    ports:
      - "5007:5007"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=5007
      - HIPCALL_API_TOKEN=${HIPCALL_API_TOKEN}
    restart: always

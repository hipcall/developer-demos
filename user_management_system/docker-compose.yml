services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
    environment:
      - DB_PATH=/app/data/database.db
      - PORT=5000
      - PUBLIC_BASE_URL=https://demos.hipcall.com.tr/user-management-system
    restart: always
